{
    "version": 2,
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "frontend/dist",
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.js"
        }
    ],
    "rewrites": [
        {
            "source": "/((?!api).*)",
            "destination": "/index.html"
        }
    ],
    "cleanUrls": true
}